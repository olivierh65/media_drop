services:
  # Alias to media_album_av_common.directory_service for backward compatibility
  media_drop.taxonomy_service:
    alias: media_album_av_common.directory_service
  media_drop.notification_service:
    class: Drupal\media_drop\Service\NotificationService
    arguments:
      - '@entity_type.manager'
      - '@plugin.manager.mail'
      - '@language_manager'

